# Docker build examiner

* `examine_local.py` Interrogates local docker engine for images with layers
containing unpinned package installs


## Requirements

* Python 2.7
* Docker engine installed locally
* docker-py package from PyPI
